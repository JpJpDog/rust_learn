naive implementation of a raft kv system by rust. based on async-raft and sled.
need zookeeper started.
start by following command.

the **kv_app.rs** and **main.rs** should be defined by the application itself.
